{
  "name": "sheets-generator",
  "version": "2.0.0",
  "private": true,
  "description": "Unit berthing sheets from Genius reports and weekend diagram prints — a single offline HTML file.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "pretest": "node build.mjs",
    "test": "node --test \"test/**/*.test.mjs\""
  }
}
